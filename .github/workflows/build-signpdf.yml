name: Build SignPDF

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'signPdf/**'
      - '.github/workflows/build-signpdf.yml'

jobs:
  build-signpdf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Build debug APK
        run: gradle -p signPdf :app:assembleDebug

      - name: Build release AAB
        run: gradle -p signPdf :app:bundleRelease

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: SignPDF-v1.1.0-debug-apk
          path: signPdf/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7

      - name: Upload release AAB
        uses: actions/upload-artifact@v4
        with:
          name: SignPDF-v1.1.0-release-aab
          path: signPdf/app/build/outputs/bundle/release/app-release.aab
          retention-days: 7
