name: DolbomOn v0.5.1 Final Store Validation

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]
    paths:
      - 'dolbomon/**'
      - 'privacy/dolbomon.md'
      - '.github/workflows/dolbomon-v051-final.yml'
  push:
    branches: [ 'feature/dolbomon-v034-exact-redesign' ]
    paths:
      - 'dolbomon/**'
      - 'privacy/dolbomon.md'
      - '.github/workflows/dolbomon-v051-final.yml'

concurrency:
  group: dolbomon-v051-final-${{ github.ref }}
  cancel-in-progress: true

jobs:
  final-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Restore v0.5.1 source
        run: bash dolbomon/ci/restore-v051.sh "$RUNNER_TEMP/dolbomon-src"

      - name: Prepare neutral visual and share regression source
        run: |
          python3 dolbomon/ci/visual-seed-v051.py "$RUNNER_TEMP/dolbomon-src"
          python3 dolbomon/ci/prepare-v046-user-flow.py "$RUNNER_TEMP/dolbomon-src"
          python3 - <<'PY'
          import os
          from pathlib import Path
          p=Path(os.environ['RUNNER_TEMP'])/'dolbomon-src/app/src/main/AndroidManifest.xml'
          s=p.read_text()
          s=s.replace('android:name=".MessageCenterActivity" android:exported="false"', 'android:name=".MessageCenterActivity" android:exported="true"')
          p.write_text(s)
          PY

      - name: Build debug and share-test APKs
        run: gradle -p "$RUNNER_TEMP/dolbomon-src" :app:assembleDebug :app:assembleDebugAndroidTest

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: API35 final daily-communication regression and screen capture
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_6
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          script: bash dolbomon/ci/run-v051-final-capture.sh "$RUNNER_TEMP/dolbomon-src"

      - uses: actions/upload-artifact@v4
        with:
          name: DolbomOn-v0.5.1-final-screens
          path: v051-final-screens
          retention-days: 7
